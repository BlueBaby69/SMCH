'use client'
import { FormEvent, useState } from 'react'
import { useRouter } from 'next/navigation'
import { createClient } from '@/lib/supabase/client'

export default function LoginPage() { const router = useRouter(); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [error,setError]=useState(''); const [loading,setLoading]=useState(false); async function submit(e: FormEvent){e.preventDefault();setLoading(true);setError('');const {error}=await createClient().auth.signInWithPassword({email,password});if(error)setError('Invalid email or password.');else router.push('/');setLoading(false)} return <main className="grid min-h-screen place-items-center bg-[#08090b] px-5 text-zinc-100"><form onSubmit={submit} className="w-full max-w-sm rounded-2xl border border-white/[.08] bg-[#0d0f12] p-7"><p className="text-xs uppercase tracking-[.18em] text-lime-300">orbit.</p><h1 className="mt-5 text-2xl font-semibold">Welcome back</h1><p className="mt-2 text-sm text-zinc-500">Sign in to manage your content engine.</p><div className="mt-7 flex flex-col gap-4"><input aria-label="Email" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" className="rounded-lg border border-white/[.09] bg-black/20 px-3 py-3 text-sm outline-none focus:border-lime-300/50"/><input aria-label="Password" type="password" required value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" className="rounded-lg border border-white/[.09] bg-black/20 px-3 py-3 text-sm outline-none focus:border-lime-300/50"/><button disabled={loading} className="rounded-lg bg-lime-300 py-3 text-sm font-semibold text-black disabled:opacity-50">{loading?'Signing in…':'Sign in'}</button>{error&&<p role="alert" className="text-xs text-rose-300">{error}</p>}</div></form></main> }
