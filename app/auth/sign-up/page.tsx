'use client'
import { FormEvent, useState } from 'react'
import { createClient } from '@/lib/supabase/client'

export default function SignUpPage() { const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [message,setMessage]=useState(''); const [error,setError]=useState(''); async function submit(e:FormEvent){e.preventDefault();setError('');setMessage('');const {error}=await createClient().auth.signUp({email,password,options:{emailRedirectTo:`${window.location.origin}/auth/callback`}});if(error)setError('Unable to create account. Check your details.');else setMessage('Check your email to confirm your account.')} return <main className="grid min-h-screen place-items-center bg-[#08090b] px-5 text-zinc-100"><form onSubmit={submit} className="w-full max-w-sm rounded-2xl border border-white/[.08] bg-[#0d0f12] p-7"><p className="text-xs uppercase tracking-[.18em] text-lime-300">orbit.</p><h1 className="mt-5 text-2xl font-semibold">Create your workspace</h1><p className="mt-2 text-sm text-zinc-500">Start reviewing and publishing with orbit.</p><div className="mt-7 flex flex-col gap-4"><input aria-label="Email" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" className="rounded-lg border border-white/[.09] bg-black/20 px-3 py-3 text-sm outline-none focus:border-lime-300/50"/><input aria-label="Password" type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password (8+ characters)" className="rounded-lg border border-white/[.09] bg-black/20 px-3 py-3 text-sm outline-none focus:border-lime-300/50"/><button className="rounded-lg bg-lime-300 py-3 text-sm font-semibold text-black">Create account</button>{message&&<p role="status" className="text-xs text-lime-300">{message}</p>}{error&&<p role="alert" className="text-xs text-rose-300">{error}</p>}</div></form></main> }
